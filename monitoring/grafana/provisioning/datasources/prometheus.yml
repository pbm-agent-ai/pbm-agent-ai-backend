# Grafana 기동 시 Prometheus 데이터소스를 자동 등록한다.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
